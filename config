ENABLED=true # Set to false to temporarily disable auto backups
EMAIL='root' # Email address or unix user to send notifications to

# Pool names should match the autobackup zfs property, e.g. 'autobackup:offsite1'
BACKUP_POOL[0]='offsite1'
BACKUP_POOL[1]='offsite2'
# BACKUP_POOL[2]='offsite3' # Uncomment this line to add additional backup pool

# TODO: make encryption optional
# ENCRYPTED=true

# TODO: if set (and ENCRYPTED=true) use KEYLOCATION for all backup pools
# Otherwise use TrueNAS cli to unlock pools
# KEYLOCATION='/path/to/zpool.key'